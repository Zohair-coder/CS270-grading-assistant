(define (lookup target environment)
  (if (null? environment) null (if (equal? target (first (first environment)))
      (first (rest (first environment))) (lookup target (rest environment))
     )
  )
);Implement Me

