(define (insert_multiple_bindings bindings table)
(cond
[(null? bindings) '()]
[(null? table) bindings]
[(not (null? table)) (append (insert_binding (first (first bindings)) table) (insert_multiple_bindings (rest bindings) table))]
 )
  );Implement Me

