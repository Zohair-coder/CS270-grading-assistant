(define (extend_table var_name current_table)
  (if (null? current_table)
    (make_bindings var_name)
    (append (insert_binding (cons var_name '(#t)) current_table) (insert_binding (cons var_name '(#f)) current_table))
  )
);Implement Me

