(define (insert_multiple_bindings bindings table)

  (cond

    [(null? table) bindings]
    [(null? bindings) '()]
    [else

     (append (insert_binding (first bindings) table) (insert_multiple_bindings (rest bindings) table))

     ]

   )

  );Implement Me

