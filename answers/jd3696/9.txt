(define (i-m-b-helper bindings table)
  (if (null? bindings)
     null
     (append (insert_binding (first (first bindings)) table) (i-m-b-helper (rest bindings) table))
     )
  )
  

(define (insert_multiple_bindings bindings table)
  (cond
    [(null? bindings) null]
    [(null? table) bindings]
    [else (i-m-b-helper bindings table)]
     )
  );Implement Me

