(define (insert_multiple_bindings bindings table)
  (cond
    [(empty? bindings) '()]
    [(empty? table) bindings]
    [else
     (append (insert_binding (first(first bindings)) table) (insert_multiple_bindings (rest bindings) table))]))

